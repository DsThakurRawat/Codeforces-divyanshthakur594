#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int n, m;
        cin >> n >> m;

        vector<string> grid(n);
        for (int i = 0; i < n; i++) {
            cin >> grid[i];   // reads a string of length m
        }

        // grid now holds n rows of m chars
    }

    return 0;
}
//https://chatgpt.com/c/693540f7-8aec-8322-8d36-a63075b2cd31 -> for details
