
// simple template-test2
// code by divyansh rawat

#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin >> n;

    string s,t;;
    cin >> s >> t;

    if(s.size()!= t.size()) {
        cout << "NO" << endl;
        return;
    }
    sort(s.begin(),s.end());
    sort(t.begin(),t.end());
    







  
}

int main() {
    int t ;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
